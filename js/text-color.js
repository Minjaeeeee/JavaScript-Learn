var heading = document.querySelector("#heading"); /* heading이라는 웹 요소를 가져온다. */
heading.onclick = function(){ /* 클릭을 했을 때 함수를 실행한다. */
    heading.style.color = "red"; /* heading이라는 웹요소에 글자 색깔을 빨간색으로 바꾼다. */
}